---

---

<section class="second-section">
    <div class="container">
        <h2 class="secondary-text text-8xl light-weight">Second Section_</h2>
        <p class="text-xl">Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos. Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos.</p>
    </div>
    <div class="background-gradient"></div>
    <div id="background-grid"></div>
</section>
<script >
    const grid = document.getElementById('background-grid');
    const gridSize = 80;
    if (!grid) return;
    const gridWidth = grid.clientWidth;
    const gridHeight = grid.clientHeight;
    const gridArea = gridSize * gridSize;
    const gridAreaWidth = gridWidth / gridSize;
    const gridAreaHeight = gridHeight / gridSize;
    for (let i = 0; i < gridArea; i++) {
        const cell = document.createElement('div');
        cell.classList.add('cell');
        grid.appendChild(cell);
    }
</script>
<style>
	.second-section {
		display: flex;
		flex-direction: column;
		align-items: center;
        width: 100%;
        height: 100vh;
        position: relative;
	}
    .container {
        position: relative;
        z-index: 10;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
        width: 100%;
        
    }
    #background-grid {
      
    position: absolute;
    inset: 0;
    height: 100%;
    width: 100%;
    background-image: linear-gradient(to right, var(--brand-primary) 1px, transparent 1px),
        linear-gradient(to bottom, var(--brand-primary) 1px, transparent 1px);
    background-size: 48px 48px;
    opacity: 0.5;
    z-index: 0;

    }
    .background-gradient {
        position: absolute;
        bottom: 0;
        height: 50%;
        width: 100%;
        background: linear-gradient(to top,  var(--brand-light-background), var(--brand-primary), var(--brand-dark-background), transparent);
        mix-blend-mode: lighten;
        /* opacity: 0.5; */
        z-index: 1;
    }
    
</style>